# OPERACIONES ARITMÉTICAS

5.2+3       # Suma
4-55        # Resta
3*-7        # Multiplicación
21/3        # División
# No existe la división inversa
9%%4         # Residuo
9%/%4       # División truncada
# No existe una función de base de Cociente hacia abajo 
# No existe una función de base de Cociente hacia arriba

# No existe una función de base MCM
# No existe una función de base MCD
abs(-32)    # Valor absoluto
sign(-32)   # Signo


3^2         # Potencia. No admite **
sqrt(17)    # Raiz cuadrada
# No existe una función de base de Raiz cúbica
exp(1)      # Exponencial
log(exp(1)) # Logaritmo natural
log10(1000) # Logaritmo base 10
log2(16)    # Logaritmo base 2
log(27, base=3)  # Logaritmo con base arbitraria

# Funciondes de redondeo
round(pi)
floor(pi)
ceiling(pi)
trunc(pi)
round(-pi)
